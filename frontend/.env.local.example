NEXT_PUBLIC_API_URL=http://localhost:8000

# Dev-only: bypass /auth/me with a mock logged-in user, useful when the
# backend isn't running or you want to work on UI without a real Google login.
NEXT_PUBLIC_MOCK_AUTH=false
