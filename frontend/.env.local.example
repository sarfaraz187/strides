NEXT_PUBLIC_API_URL=http://localhost:8000

# Dev-only: bypass /auth/me (not implemented on the backend yet) with a mock
# logged-in user, so the dashboard/chat screens are reachable locally.
NEXT_PUBLIC_MOCK_AUTH=false
